# AI 작업 분할 지침

이 프로젝트는 AI에게 한 번에 “웹앱 만들어줘”라고 맡기지 않는다. 반드시 작업 단위를 쪼개서 진행한다.

## 고정 스펙

- 프로젝트명: `네 화분 찾아가라`
- 부제: `녹색지대 화분 관상소`
- 결과 유형 4개:
  - `succulent`: 일감호 잔잔 다육이
  - `cactus`: 스탠딩존 생존 선인장
  - `hoya`: 봄밤 감성 하트호야
  - `fishbone`: 구석부스 취향 피쉬본
- 질문: 7개
- 답변 매핑:
  - A -> `succulent`
  - B -> `cactus`
  - C -> `hoya`
  - D -> `fishbone`
- 동점 처리 우선순위:
  - `succulent`, `cactus`, `hoya`, `fishbone`

질문/답변/결과/로딩 문구는 `src/data/coolbti.ts`를 기준으로 한다. AI가 문구를 재작성하거나 결과 유형을 추가하면 안 된다.

## 권장 진행 순서

1. 데이터 파일만 검수한다.
2. 화면 구조만 만든다.
3. 기능 로직만 연결한다.
4. 디자인은 화면별로 하나씩 수정한다.
5. 식물학생증 저장 기능을 별도 작업으로 처리한다.
6. Supabase/Vercel 연결은 기능 구현 이후 별도 작업으로 처리한다.
7. 최종 QA를 별도 작업으로 처리한다.

## 금지 방식

- 전체 앱을 한 번에 다시 만들기
- 문구를 AI가 임의로 바꾸기
- 결과 유형 추가
- 점수 매핑 변경
- 기존 정상 로직을 디자인 수정 중 같이 건드리기
- 디자인 수정과 DB 연결을 같은 작업으로 묶기

## 화면별 수정 원칙

- LandingScreen만 수정할 때는 랜딩만 수정한다.
- QuizScreen만 수정할 때는 질문 화면만 수정한다.
- LoadingScreen만 수정할 때는 로딩/가챠 화면만 수정한다.
- ResultScreen만 수정할 때는 결과 화면만 수정한다.
- StudentCardScreen만 수정할 때는 식물학생증만 수정한다.
- BoothInfoScreen만 수정할 때는 부스 정보만 수정한다.

현재 앱은 아직 단일 `App.tsx` 중심 구조다. 구조 분리 작업을 할 경우에도 먼저 데이터/로직 QA를 통과한 뒤 진행한다.

## 최종 QA 기준

- 메인 타이틀이 `네 화분 찾아가라`인가?
- 부제가 `녹색지대 화분 관상소`인가?
- 질문은 7개인가?
- 각 질문 답변은 4개인가?
- A/B/C/D 매핑이 고정되어 있는가?
- 결과 유형은 정확히 4개인가?
- 로딩 문구가 순서대로 나오는가?
- CTA가 있는가?
- 식물학생증 흐름을 추가할 경우 입력값이 카드에 반영되는가?
- 모바일에서 텍스트가 잘리지 않는가?
